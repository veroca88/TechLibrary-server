{{!-- <h1>Profile</h1>

{{#if user}}
<img src="" class="profile-img center">
<br>
<h1>{{user.username}}'s Profile</h1>
<p>Here is some info of your account!!</p>



<div class="form-row justify-content-center">
    <div class="form-group col-md-4">
        <label>First Name</label>
        <label class="form-control">{{user.firstName}}</label>
    </div>
    <div class="form-group col-md-4">
        <label>Last Name</label>
        <label class="form-control">{{user.lastName}}</label>
    </div>
</div>
<div class="form-row justify-content-center">
    <div class="form-group col-md-8">
        <label>Email</label>
        <label class="form-control">{{user.email}}</label>
    </div>
</div>

{{else}}
<p>Still no logged in user, sorry!</p>
{{/if}} --}}

{{#if user}}
<div class="container">
    <div class="row">
        <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
            <div class="card card-signin my-5">
                <div class="card-body">
                    <h5 class="card-title text-center">{{user.username}}'s Profile</h5>
                    <p>Here you can find info about your account!</p>
                    <div class="form-label-group">
                        <label>Username</label>
                        <label class="form-control">{{user.firstName}}</label>
                    </div>
                    <div class="form-label-group">
                        <label>Email</label>
                        <label class="form-control">{{user.email}}</label>
                    </div>
                    <hr class="my-4">
                    {{#if message}}
                    <div class="error-message">{{ message }}</div>
                    {{/if}}
                    <a class="btn btn-lg btn-warning btn-block" href="/">Back Home</a>
                    <br>
                </div>
            </div>
        </div>
    </div>
</div>
{{/if}}